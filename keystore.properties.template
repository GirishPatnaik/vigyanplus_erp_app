# Copy this file to keystore.properties (same folder) and fill in real values.
# keystore.properties itself is git-ignored — never commit real signing credentials.
storeFile=../keystore/vigyan_release.keystore
storePassword=CHANGE_ME
keyAlias=vigyan_jrcollege
keyPassword=CHANGE_ME
